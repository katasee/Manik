# Xcode
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/

# Swift Package Manager local checkout caches (not Package.resolved — keep that for reproducible builds)
.build/
.swiftpm/xcode/package.xcworkspace/xcuserdata/

# Firebase config — regenerate per environment from Firebase Console, don't commit
GoogleService-Info.plist

# Superpowers per-feature working artifacts — throwaway, deleted once the feature ships.
# The product-wide MVP spec is the one permanent file here and stays tracked.
docs/superpowers/plans/
docs/superpowers/specs/*
!docs/superpowers/specs/2026-07-15-manik-mvp-design.md

# macOS
.DS_Store
